const getters = {
  teamLogo:state => state.site.teamLogo,
  teamTitle:state => state.site.teamTitle,
  siteTitle:state => state.site.siteTitle,
  ISPN:state => state.site.ISPN,
  siteCreateTime:state => state.site.siteCreateTime
}
export default getters
